# SQLite Native Plugin Intake

Place SQLite plugin sources or vendor notes here before platform binaries are
added under `Assets/Plugins/Android` or `Assets/Plugins/iOS`.

The runtime repository is currently scaffolded. Native persistence still requires
plugin approval and import.
