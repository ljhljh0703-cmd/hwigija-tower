# Model Training Workspace

Use this folder for local training/evaluation artifacts that are safe to track.

- `datasets/`: JSONL training and eval sets.
- `adapters/`: small adapter outputs if license and size allow tracking.
- `evals/`: reports, sampled outputs, latency measurements.

Do not commit full base model checkpoints without explicit license and storage approval.
