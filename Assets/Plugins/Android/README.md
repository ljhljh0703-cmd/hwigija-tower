# Android Native Plugins

Place Android native libraries here after approval.

Expected future files:

- MLC runtime library for `mlc_llm_unity`.
- SQLite native library if required by the selected persistence package.

Do not add unapproved binaries.
