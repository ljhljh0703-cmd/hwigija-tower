# 반려 기록 (전 화면 공통 · append-only)

> 스킬 `game-ui-from-concept` 4단 산출. 화면별로 쪼개지 않는다.
> **같은 반려가 두 번 나오면 그건 제약으로 올린다.** 뒤로 갈수록 라운드가 줄어드는 유일한 방법.

## 형식

```
### YYYY-MM-DD · <화면> · 라운드 N
- 위반: <제약 ID 또는 불변식>
- 실제: <측정값 또는 관찰>
- 기대: <사양 값>
- 처리: 3단 재위탁 / 사양 수정 후 재시작 / 작가 판정 대기
- 승격 후보: yes|no  (두 번째 발생 시 yes)
```

## 승격된 제약

- **[후보 · 1회] 제약이 의도보다 넓은지 검사한다** — 2단 완료 판정에 추가 검토. 근거 = 2026-09-01 로비 라운드 1. 한 번 더 나오면 확정 승격

---

## 기록

### 2026-09-01 · 로비 · 라운드 1
- 위반: **사양 자체 모순** (L-03 ⟂ titleMark)
- 실제: 교집합 x .35~.65, y .46~.55. `titleMark`(x .10~.90, y .46~.58)가 L-03 금지영역(x .35~.65, y .10~.55)을 침범
- 기대: L-01~L-07 전부 통과 — **현재 JSON으로는 불가능**
- 처리: **사양 수정 후 재시작.** 보호영역을 줄이지 않고 제약 문구를 고쳤다. L-03 을 「불투명 패널·interactive 요소」로 한정하고 `layer=title` 을 면제. 대신 L-08 신설(타이틀은 불투명 배경 없이)
- 근본 원인: **1단(제약 번역) 실패.** layoutIntent 는 「버튼이 배경을 가리지 않는다」였는데 제약을 「어떤 UI도」로 넓게 썼다. 검사 가능하게는 썼으나 의도보다 넓었다
- 승격 후보: **yes** — 「제약이 의도보다 넓은가」를 2단 완료 판정에 추가할 것
- 비고: Codex 가 코드·테스트·캡처를 실행하지 않고 STOP 했다. 거짓 통과를 안 만든 판단이 맞다

### 2026-09-01 · 로비 · 관찰 (반려 아님)
- **로비는 이미 `LobbyController.cs` 로 `PrototypeHud.cs` 에서 분리돼 있었다.** 사양 §4-1「분리한다」는 이미 충족 상태
- 뜻: 5,021줄이 통짜가 아니라 일부는 이미 갈라져 있다. 12화면 분해 견적(4~7라운드)이 낮아질 여지
- 후속: 라운드 2에서 **경계 검증**으로 대체 — 로비를 바꿔도 다른 화면 테스트가 안 깨지는지 확인. 분리 작업이 아니라 확인 작업이다
- 원격 기준선 확정: `origin/Proto@5adc2e1`

### 2026-09-01 · 로비 · 라운드 2 · 사양 결함 1건 (구현 결함 0건)
- 위반: **사양이 데이터 모델에 없는 것을 요구** — `runStatus.content` 「회차」
- 실제: 현재 save 스키마에 회차 누적 필드가 없다. Codex 가 새 저장 스키마를 만들지 않고 보고했다 (맞는 판단)
- 처리: **사양 수정** v1.2 — content 를 「저장 유무 · 최고 도달 층 · 기억 조각」으로. 회차 누적은 별건 분리
- 근본 원인: **1단 실패 2회째.** 제약·내용을 쓸 때 「구현 가능한가」를 안 봤다. 라운드 1은 사양 내부 모순, 이번은 사양 ⟂ 데이터 모델
- 승격 후보: **yes (2회째 → 확정 승격)** — 2단 완료 판정에 「사양이 요구하는 데이터가 실제로 존재하는가」 추가

### 2026-09-01 · 로비 · 라운드 2 · 자산 부재 2건 (반려 아님)
- 명조 계열 TMP 폰트 에셋 리포에 없음 → `LegacyRuntime.ttf` fallback, 시각 일치 미검증
- `tower_deep` 아트 없음 → `deepRun` variant `status: blocked` 로 표기. **임의 대체하지 않은 것이 맞다**
- 처리: 사양 v1.2 `assetFallbacks` 절에 명시. 코드 문제가 아니라 자산 문제로 분리

### 2026-09-01 · 로비 · 라운드 2 · 독립 채점 결과
- 사양 자체 검사 **8/8 PASS**
- 사양 ⟂ 코드 상수 대조 **12/12 PASS** (LobbyLayout.cs 상수를 정규식 추출해 `lobby_actual_layout.json` 생성)
- **Codex 자기보고와 일치.** 자기보고 불신 원칙에 따라 별도 도구로 재현했고 어긋남 0
- 미검증: Unity EditMode·PlayMode·PNG 캡처 (라이선스 exit 198)

### 2026-09-01 · 휴식 · 사양 작성 중 · 자체 채점 (발송 전 검출)
- 위반: **R-05** — `departButton` 높이 0.048 = 92px < 96px
- 처리: 사양 발행 전 0.052 로 정정. **코덱스에게 가지 않았다**
- 근본 원인: 1단 실패 3회째. 다만 **이번은 채점기가 발송 전에 잡았다**
- 의미: 승격 확정된 「사양이 요구하는 것이 실현 가능한가」 검사가 자동화되면서 라운드 비용이 0이 됐다. R1·R2 는 각각 라운드 하나를 썼다
- 승격 후보: no (이미 승격됨 · 이번은 그 효과 확인)

### 2026-09-01 · 로비·전투 · 정적 레인 종결 · 독립 재현
- 브랜치 `origin/codex/hwigi-lobby-ui-pilot-r3` · 커밋 `120f37c`(로비) `e9d5454`(전투)
- 로비: 사양 8/8 + actual 12/12 **PASS**
- 전투: C-01~C-07 + actual 27/27 **PASS**, C-08 은 런타임 대비 측정 대기
- Codex 자기보고와 차이 **0건**
- Codex 가 `DumpLayout` 순수 C# 도구와 `*LayoutContract.cs` 를 만들어 **Unity 없이 actual 을 뽑는 경로**를 열었다. §14-2 요청 그대로
- 미검증: Unity EditMode·PlayMode·PNG 캡처 (라이선스)

### 2026-09-01 · 휴식 · 위탁 직전 검사 (발송 전 검출 · 2건)
- 위반 ①: **사양이 데이터 모델에 없는 것을 요구** — `mataiosSanityBar` · `variants.*.trigger`「마타이오스 이성 0.3~0.6 / < 0.3」
- 실제: `Assets/_Project/Scripts` 전수 `Sanity` **0건**. 「이성」 대응 필드는 `PrototypeRunSnapshot.Mental`(int, −100~+100) 하나뿐이고 **플레이어 것**이다. 마타이오스 전용 이성 값은 없다. 그의 상태 축은 GDD 상 **붕괴도**(Glitch / S0~S5)이고 코드는 `ModifyGlitchLevel` — 별개 축이다
- 기대: 사양이 트리거로 쓸 정규화 0~1 값
- 처리: **작가 판정 대기** (축 이름·범위 결정 필요)
- 근본 원인: **1단 실패 4회째, 같은 유형 2회째**(R2 `runStatus` 회차와 동종). 승격된 2단 판정이 이번에도 잡았고 **라운드 소모 0**

- 위반 ②: **`D-029` 잠긴 결정 충돌** — `variants.broken.reward`「대화 선택 시 즉시 이성 회복 + **다음 전투 1턴 강화**」
- 실제: D-029(2026-05-22 LOCKED) = 붕괴도는 전투 타이머·능력 발동 조건·공격력 modifier·시너지 배율의 기본 입력으로 **사용하지 않는다**. 붕괴 상태에서 파생된 보상이 다음 전투를 강화하면 관계 축이 전투 modifier 로 되돌아온다
- 기대: 회복 보상이 관계·회복 축 안에서 닫힐 것
- 처리: **작가 판정 대기.** 사양을 고치는 것은 스킬 4단(사양 수정 후 3단 재시작)이지만, 이 절은 `D-NNN` 영역이라 AI 가 단독으로 못 고친다
- 🔴 **동일 문구가 `combat_layout_spec_v2.json` `variants.broken.reward` 에도 있다**(「즉시 이성 회복 + 1턴 강화 효과」). 그 사양은 **27/27 PASS 로 통과했다** — 채점기가 좌표만 보고 `variants` 의 보상·서사 절은 안 본다. 다만 `combat_actual_layout.json` 에 `variants` 가 없고 `CombatLayoutContract.cs` 에도 strained/broken 이 없어 **코드로 실현된 적은 없다.** 되돌릴 것 없음
- 승격 후보: **yes** — 「사양의 `variants.reward`·`recovery` 절이 잠긴 결정과 충돌하지 않는가」를 2단 완료 판정에 추가. 좌표 채점기로는 영원히 안 잡힌다

### 2026-09-01 · 휴식·전투 · 사양 수정 (작가 판정 A-1 · B-1 반영)
- 판정: **A-1** 마타이오스 상태 축을 붕괴도로 교체 / **B-1** `broken` 보상에서 전투 강화 절 삭제
- 휴식 → **v1.1**, 전투 → **v2.1**. 자체 채점 재실행 = 휴식 **9/9 PASS** · 전투 **8 PASS · C-08 SKIP**
- 축 실사: `PrototypeRunSnapshot.GlitchLevel`(int 0~100) **실재**. `NpcStage` enum·`NpcStateMachine` 은 있으나 `AttachNpcStateMachine` 호출처가 **테스트뿐**이고 `NpcStateMachineDefinition` 에셋이 없다 → 프로토타입 런타임에서 null. 바인딩 대상 아님
- 🔴 **파생 검출 3건째** — GDD D-012 P2 「`Glitch`/붕괴도 키를 normal UI 에 노출하지 않는다」. 붕괴도를 게이지·숫자로 그리면 이 조항 위반이라 `mataiosSanityBar` 를 **`mataiosStateLabel`(비수치 authored 문구)** 로 교체. 규칙 신설 = 휴식 `R-09` · 전투 `C-09`(`textAbsent` 「붕괴도」)
- 🔴 **임계값(붕괴도 → 표시 단계)은 정하지 않았다.** `thresholds.*: undecided` 로 두고 variants 를 `status: blocked` 로 내렸다. 아트 4종 미입고와 합쳐 어차피 비활성이므로 라운드를 막지 않는다. 확정 시 OQ-022 식 temporary contract·코드 hard-code 금지
- 🆕 **`dataBinding` 블록 신설**(휴식 사양). 슬롯마다 런타임 출처·타입·범위·정규화 식을 적는다. 승격된 2단 판정「사양이 요구하는 데이터가 실제로 존재하는가」의 **기계화**. `sanityChip` 은 `Mental`(−100~+100)이라 0~1 이 아니다 — 정규화 식을 사양이 들고 코드가 추측하지 않는다
- ⚠️ **전투 회귀 1건 발생(의도된 것)** — 슬롯 rename 으로 `combat_actual_layout.json` 대조가 27/27 → **26/27 · `mataiosStateLabel` 런타임 보고에 없음**. 코드가 존재하지 않는 값에 붙어 있었다는 뜻이고, 채점기가 드리프트를 제대로 잡은 것이다. 조치 = `CombatLayoutContract.cs` 슬롯 1개 rename + `DumpLayout` 재실행
- 승격 후보: **yes** — 「사양 슬롯마다 런타임 출처가 적혀 있는가」를 2단 완료 판정에 추가. `dataBinding` 이 그 서식이다

### 2026-09-01 · 정정 · 「워크트리가 날아갔다」는 내 오판이었다
- 주장: 「`/private/tmp/hwigi-lobby-ui-pilot-20260901` 워크트리가 소멸했다」 (본 세션 초반 보고)
- 근거로 쓴 것: `git worktree list` 의 `prunable` 표시와 `ls -d /private/tmp/...` 실패
- 🔴 **둘 다 리눅스 VM 에서 읽었다.** `device_bash` 는 맥이 아니라 그 안의 VM 이고, 연결 폴더 밖은 아예 다른 파일시스템이다. **맥의 `/private/tmp` 는 이 VM 에 존재한 적이 없다** — 그러니 「없다」가 아니라 「내가 볼 수 없다」였다. `prunable` 도 VM 안의 git 이 같은 이유로 붙인 표시다
- 반증한 것: 작가가 맥에서 `git worktree prune` 을 돌렸는데 **등록부가 안 지워졌다**(`.git/worktrees/hwigi-lobby-ui-pilot-20260901` 잔존, `locked` 파일 없음). prune 은 디렉터리가 실제로 없을 때만 지운다 → **맥에는 살아 있다**
- 파생 피해: 브랜치가 그 워크트리에 물려 있어 `git worktree add` 가 거부됐고, 그게 v1·v2 실패의 진짜 원인이었다. v1 은 빈 폴더만 만들고 `cd` 는 성공시켜 이후 git 4개가 리포 밖에서 돌았다(오류 검사 부재 = 내 스크립트 결함)
- 처리: v3 에서 **`git worktree move`** 로 tmp 작업물을 보존한 채 안전 경로로 옮긴다. 삭제·재생성이 아니다
- 승격 후보: **yes** — 「연결 폴더 밖 경로에 대해 `device_bash` 로 부재를 단정하지 않는다. 없다고 말하려면 맥에서 확인한다」. 색인 계열 교훈(`feedback_check_the_index_first`)의 파일시스템 판본

### 2026-09-01 · 휴식 R1 · ③Gate 독립 재현 — 조건부 통과(정적) · `flagged` 유지
**재현 결과 — 자기보고와 차이 0건 (3라운드 연속)**

| 검사 | 코덱스 보고 | 내 재현 |
|---|---|---|
| 휴식 사양 R-01~R-09 | 9/9 | **9/9 PASS** |
| 휴식 actual 대조 | 20/20 | **20/20 PASS** |
| 전투 사양 | 8 PASS · C-08 SKIP | **동일** |
| 전투 actual 대조 | 27/27 | **27/27 PASS** (26/27 에서 복귀) |
| 로비 actual 대조 | — | **12/12 PASS**(무회귀 확인) |

**잘한 것 넷 (명시)**
1. **캡처 실패를 정적 actual 로 대체하지 않았다.** RETURN 이 `rest_actual_layout.json` 을 「source=`RestLayoutContract.cs`, **Unity 미실행 정적 actual**」로 명기했다. 내가 PNG 를 전수 검색한 결과 실제로 **캡처 0장**(검색에 걸린 png 는 전부 ml-agents 패키지 자산). 보고와 실물이 같다
2. **C-08 SKIP 유지.** 기본 상태 대비로 대체 측정하지 않았다
3. **커밋·push 안 함.** 작업물은 워크트리에 그대로
4. **D-012·D-029 준수** — 붕괴도 delta·`MATAIOS_TRAINING_BUFF_ACTIVE`·저장 호출 무변경

**⚠️ 정적 actual 의 한계를 기록한다 (이 라운드가 증명하지 *못한* 것)**
`20/20`·`27/27` 은 **사양 ⟂ C# 상수** 대조다. 런타임이 그 좌표로 실제 배치하는지는 **여전히 미검증**이다. 두 산출이 같은 라운드에서 나왔으므로 이 숫자를 「화면이 맞다」로 읽으면 안 된다. §13-2 런타임 writer 는 배선됐으나 캡처가 그 앞에서 멈춰 한 번도 안 돌았다.

**실패 39건 분리 — 내 독립 확인**
- **22건 = 단일 환경 원인.** `com.unity.ml-agents/Samples` meta 누락 로그가 TestRunner 에서 error 로 승격돼 실패 처리. **단언 실패가 아니다.** 이 한 건만 잡으면 EditMode 181/211 → 약 203/211
- **로비 no-save 격리 실패(+캡처 차단) = ⓒ 확정.** 주장이 아니라 **mtime 으로 확인** — `LobbyController.cs`·`LobbySmokeTests.cs` 최종 수정 **08-31 23:26~23:27**, 이번 라운드 무접촉. 로비는 R2/R3 산출이고 첫 런타임 실행이 이제야 드러낸 것
- **`PrototypeRoomSmokeTests.cs` 는 이번에 수정됐다.** 다만 diff 를 뜬 결과 **휴식 라벨 단언을 사양의 `content` 문자열로 맞춘 것뿐**(「단련 · 다음 전투 보너스」·「휴식 · 체력 회복」 = 사양 원문 그대로). **테스트가 코드가 아니라 사양을 따라갔다** — ③표「채점 기준이 채점 대상을 따라간다」위반 아님. 그리고 이 파일의 실패 6건은 **맵 노드 탐색 단언**이라 라벨 단언보다 앞에서 터진다 → 그 수정이 원인이 아니다
- 🔴 **한계 자인**: ⓒ 를 **기준선 diff 로 못 증명했다.** 브랜치의 옛 XML 은 52·57 케이스뿐이고 이번은 211 이라 범위가 달라 교집합 비교가 무의미하다. 위 판정은 **근본 원인 판독과 mtime** 에 근거한 것이지 기준선 실측이 아니다
- 🧪 **미검증 가설**: 로비 no-save 누수와 맵 진행 실패(`ENC_REST_01`·`CHOICE_COMBAT_1_ENGAGE`·`CHOICE_SHOP_1_BUY_ITEM` 미발견)가 **런/저장 상태의 테스트 간 누수**라는 한 뿌리일 수 있다. `PrototypeRunSaveStore` 는 `Application.persistentDataPath` 파일이고 `Delete()` 후에도 Continue 가 활성이었다. **가설이지 판정이 아니다**

**승격 후보: yes** — 「`actual` 산출이 런타임에서 나온 것인지 코드 상수에서 나온 것인지 파일 안에 적는다」. RETURN 이 이번엔 본문에 적었지만 JSON 자체엔 없다. `source` 필드를 사양 스키마에 넣어 채점기가 구분하게 한다

### 2026-09-01 · lobby · 라운드 2 · 자동 채점
- 위반: **visible:titleMark**
  - 캡처에 없음
- 위반: **visible:primaryAction**
  - 캡처에 없음
- 위반: **slot:towerArt**
  - x 사양 0.000 ⟂ 실제 -1.500 (허용 0.0042)
  - y 사양 0.000 ⟂ 실제 -1.500 (허용 0.0042)
  - w 사양 1.000 ⟂ 실제 4.000 (허용 0.0042)
  - h 사양 0.700 ⟂ 실제 4.000 (허용 0.0042)
- 위반: **slot:titleMark**
  - x 사양 0.100 ⟂ 실제 -1.180 (허용 0.0042)
  - y 사양 0.460 ⟂ 실제 -0.540 (허용 0.0042)
  - w 사양 0.800 ⟂ 실제 3.360 (허용 0.0042)
  - h 사양 0.120 ⟂ 실제 0.400 (허용 0.0042)
- 위반: **slot:primaryAction**
  - x 사양 0.100 ⟂ 실제 -0.940 (허용 0.0042)
  - y 사양 0.690 ⟂ 실제 0.500 (허용 0.0042)
  - w 사양 0.800 ⟂ 실제 2.880 (허용 0.0042)
  - h 사양 0.078 ⟂ 실제 0.280 (허용 0.0042)
- 처리: 3단 재위탁 (사양 수정 아님)
- 승격 후보: no

### 2026-09-01 · combat · 라운드 2 · 자동 채점
- 위반: **visible:sanityChip**
  - 캡처에 없음
- 위반: **visible:threatReadout**
  - 캡처에 없음
- 위반: **visible:skillButton**
  - 캡처에 없음
- 위반: **slot:floorChip**
  - 런타임 보고에 없음
- 위반: **slot:sanityChip**
  - 런타임 보고에 없음
- 위반: **slot:hpChip**
  - 런타임 보고에 없음
- 위반: **slot:goldChip**
  - 런타임 보고에 없음
- 위반: **slot:threatReadout**
  - 런타임 보고에 없음
- 위반: **slot:enemyPanel**
  - x 사양 0.063 ⟂ 실제 0.095 (허용 0.0042)
  - y 사양 0.186 ⟂ 실제 0.118 (허용 0.0042)
  - w 사양 0.874 ⟂ 실제 0.810 (허용 0.0042)
  - h 사양 0.368 ⟂ 실제 0.404 (허용 0.0042)
- 위반: **slot:enemyTitle**
  - x 사양 0.100 ⟂ 실제 0.144 (허용 0.0042)
  - y 사양 0.196 ⟂ 실제 0.130 (허용 0.0042)
  - w 사양 0.800 ⟂ 실제 0.712 (허용 0.0042)
  - h 사양 0.038 ⟂ 실제 0.057 (허용 0.0042)
- 위반: **slot:enemyHpBar**
  - y 사양 0.238 ⟂ 실제 0.193 (허용 0.0042)
  - w 사양 0.704 ⟂ 실제 0.696 (허용 0.0042)
- 위반: **slot:enemyStatusChips**
  - x 사양 0.220 ⟂ 실제 0.152 (허용 0.0042)
  - y 사양 0.266 ⟂ 실제 0.217 (허용 0.0042)
  - w 사양 0.560 ⟂ 실제 0.696 (허용 0.0042)
  - h 사양 0.028 ⟂ 실제 0.020 (허용 0.0042)
- 위반: **slot:enemyImage**
  - x 사양 0.200 ⟂ 실제 0.136 (허용 0.0042)
  - y 사양 0.300 ⟂ 실제 0.215 (허용 0.0042)
  - w 사양 0.600 ⟂ 실제 0.729 (허용 0.0042)
  - h 사양 0.246 ⟂ 실제 0.295 (허용 0.0042)
- 위반: **slot:damageEffectOverlay**
  - x 사양 0.400 ⟂ 실제 0.338 (허용 0.0042)
  - y 사양 0.320 ⟂ 실제 0.288 (허용 0.0042)
  - w 사양 0.200 ⟂ 실제 0.324 (허용 0.0042)
  - h 사양 0.100 ⟂ 실제 0.065 (허용 0.0042)
- 위반: **slot:playerCard**
  - x 사양 0.055 ⟂ 실제 0.128 (허용 0.0042)
  - y 사양 0.572 ⟂ 실제 0.697 (허용 0.0042)
  - w 사양 0.420 ⟂ 실제 0.356 (허용 0.0042)
  - h 사양 0.152 ⟂ 실제 0.134 (허용 0.0042)
- 위반: **slot:playerHpBar**
  - x 사양 0.075 ⟂ 실제 0.142 (허용 0.0042)
  - y 사양 0.678 ⟂ 실제 0.811 (허용 0.0042)
  - w 사양 0.380 ⟂ 실제 0.328 (허용 0.0042)
  - h 사양 0.016 ⟂ 실제 0.009 (허용 0.0042)
- 위반: **slot:playerSanityBar**
  - 런타임 보고에 없음
- 위반: **slot:mataiosCard**
  - x 사양 0.525 ⟂ 실제 0.516 (허용 0.0042)
  - y 사양 0.572 ⟂ 실제 0.697 (허용 0.0042)
  - w 사양 0.420 ⟂ 실제 0.356 (허용 0.0042)
  - h 사양 0.152 ⟂ 실제 0.134 (허용 0.0042)
- 위반: **slot:mataiosHpBar**
  - x 사양 0.545 ⟂ 실제 0.530 (허용 0.0042)
  - y 사양 0.678 ⟂ 실제 0.811 (허용 0.0042)
  - w 사양 0.380 ⟂ 실제 0.328 (허용 0.0042)
  - h 사양 0.016 ⟂ 실제 0.009 (허용 0.0042)
- 위반: **slot:mataiosStateLabel**
  - 런타임 보고에 없음
- 위반: **slot:attackButton**
  - x 사양 0.060 ⟂ 실제 0.219 (허용 0.0042)
  - y 사양 0.760 ⟂ 실제 0.870 (허용 0.0042)
  - w 사양 0.270 ⟂ 실제 0.109 (허용 0.0042)
  - h 사양 0.110 ⟂ 실제 0.061 (허용 0.0042)
- 위반: **slot:defendButton**
  - x 사양 0.365 ⟂ 실제 0.445 (허용 0.0042)
  - y 사양 0.760 ⟂ 실제 0.870 (허용 0.0042)
  - w 사양 0.270 ⟂ 실제 0.109 (허용 0.0042)
  - h 사양 0.110 ⟂ 실제 0.061 (허용 0.0042)
- 위반: **slot:skillButton**
  - y 사양 0.760 ⟂ 실제 0.870 (허용 0.0042)
  - w 사양 0.270 ⟂ 실제 0.109 (허용 0.0042)
  - h 사양 0.110 ⟂ 실제 0.061 (허용 0.0042)
- 위반: **slot:combatLog**
  - x 사양 0.060 ⟂ 실제 0.095 (허용 0.0042)
  - y 사양 0.888 ⟂ 실제 0.531 (허용 0.0042)
  - w 사양 0.700 ⟂ 실제 0.810 (허용 0.0042)
  - h 사양 0.056 ⟂ 실제 0.138 (허용 0.0042)
- 위반: **slot:itemInspectButton**
  - x 사양 0.790 ⟂ 실제 0.798 (허용 0.0042)
  - y 사양 0.888 ⟂ 실제 0.824 (허용 0.0042)
  - w 사양 0.150 ⟂ 실제 0.069 (허용 0.0042)
  - h 사양 0.056 ⟂ 실제 0.030 (허용 0.0042)
- 처리: 3단 재위탁 (사양 수정 아님)
- 승격 후보: no

### 2026-09-01 · lobby · 라운드 2 · runtime actual writer 정정
- 직전 lobby 자동 반려는 화면 좌표 반려가 아니다. writer가 `FindObjectsInactive.Include`의 **비활성 legacy** `Lobby Portrait Safe Area`/슬롯을 먼저 집어 `source: runtime` JSON에 기록한 측정 결함이었다.
- 조치: `PortraitUiScreenshotQaTests` writer가 캡처 기준 root의 자식만 찾고, 활성 노드를 우선하며 inactive 노드는 같은 root 안에서만 fallback으로 고르도록 수정했다.
- 재캡처: `01_lobby_no_save.png` 재생성, `lobby_actual_layout.json` top-level `source: runtime`, runtime 대조 **12/12 PASS**. 앞선 lobby 자동 반려는 audit으로 남기되 layout 판정에는 사용하지 않는다.
- 승격 후보: no

### 2026-09-01 · 런타임 R2 · ③Gate — **전투 반려** · 로비·휴식 통과
**🔴 이 하네스가 만들어진 이유가 여기서 처음 드러났다.**

- 위반: **`combat_layout_spec_v2.json` 이 화면에 구현된 적이 없다.** 계약 파일만 있고 런타임 배선이 없다
- 실제(내 독립 확인, 반증 시도 포함):
  - `CombatLayoutContract` 의 22 슬롯 중 **18개의 이름이 `PrototypeHud.cs` 에 단 한 번도 안 나온다**
  - 반증 시도 — 상수로 참조할 가능성: `PrototypeHud.cs` 에 `CombatLayoutContract` 참조 **0건**
  - `CombatLayoutContract` 를 참조하는 파일은 **전 리포에서 1개, 그것도 테스트**(`LobbyLayoutSpecTests.cs`)
  - 대조군 — 휴식은 배선돼 있다: `PrototypeHud.cs:85` `[SerializeField] RestUiController`, `:2643` `AddComponent<RestUiController>()`
  - 전투 UI 모듈이 없다. `RestLayout.cs`·`RestUiController.cs` 는 있는데 `Combat*Controller.cs` 는 없다
- 기대: 사양 v2 대로 배치된 전투 화면
- 🔴 **왜 정적 게이트가 못 잡았나**: `27/27 PASS` 는 **사양 ⟂ 계약 상수** 대조였고 **둘 다 같은 라운드에서 같은 손이 썼다.** 서로를 베낀 두 문서가 일치하는 것을 「구현됐다」로 읽었다. 화면은 한 번도 안 봤다
- 런타임 실측이 그것을 드러냈다 — 전투 runtime 대조 **2 PASS · 25 FAIL**, v2 신설 슬롯 7종(`floorChip`·`sanityChip`·`hpChip`·`goldChip`·`threatReadout`·`playerSanityBar`·`mataiosStateLabel`)이 **런타임 보고에 아예 없다**(존재하지 않으니까). `combatLog` Δy **−0.357** = 구 레이아웃 그대로
- 처리: **`QUEUE.md` 2번 전투를 「통과(정적)」에서 반려로 내린다.** 3단 재위탁
- 승격 후보: **yes (확정)** — **「정적 actual 이 런타임 배선을 증명하지 않는다. 계약 파일이 화면에 연결됐는지를 2단·4단 완료 판정에 넣는다」**
  기계 검사 가능: *계약 타입을 참조하는 비-테스트 파일이 1개 이상 존재하는가.* 이번 경우 0이었다. `check_layout.py` 가 아니라 별도 검사가 필요하다

**코덱스 R2 검증 — 자기보고와 차이 0건 (4라운드 연속)**

| 항목 | 보고 | 내 확인 |
|---|---|---|
| 전역 `LogAssert.ignoreFailingMessages` 미사용 | 미사용 | `Assets/` 전수 **0건** ✅ |
| 무시 패턴 범위 | 메시지 1종 | `^Asset Packages/com\.unity\.ml-agents/Samples...$` **앵커된 정규식 1개** ✅ |
| 단언 약화 여부 | 유지 | `Assert.IsFalse(continueButton.interactable)` **line 72 보존** · `Ignore`/`Explicit` **0건** ✅ |
| 기준선 = R1 실패 집합 동일 | EditMode 30/30 · PlayMode 8/8 | XML 리포 내 보존, 해시 병기 |
| 캡처·런타임 actual | 생성됨 | RETURN 에 해시 병기 (파일은 `/private/tmp` — **내 마운트 밖이라 미재현**) |

**잘한 것 셋**
1. **내 가설을 반증하고 그렇게 적었다.** 「저장 상태 누수」는 **틀렸고**, 진짜 원인은 `LobbyController` 가 비활성 legacy Canvas 의 동명 버튼을 바인딩한 것이었다. 가설에 코드를 맞추지 않았다
2. **측정 도구를 고친 것을 숨기지 않았다.** 최초 로비 5 FAIL 은 writer 가 비활성 legacy RectTransform 을 집은 계측 오류였고, 자동 반려 기록을 **삭제하지 않고 정정 append** 했다. 도구 수정 자체는 정당하나 — **결과를 본 뒤 계측기를 고친 것**이므로 이 절에 명시해 둔다
3. **전투 25 FAIL 을 고치지 않고 수치로 보존했다.** 고쳤으면 위 발견이 묻혔다

**⚠️ 내 검증 한계**: 런타임 actual JSON 과 PNG 는 `/private/tmp` 에 있어 연결 폴더 밖이다. **슬롯별 Δ 수치는 재현하지 못했고 RETURN 표를 인용한 것이다.** 다만 위 「전투 미배선」 판정은 Δ 표가 아니라 **리포 안 소스 코드**로 독립 확인했다. 다음 라운드부터 런타임 산출을 리포 안으로 낼 것

### 2026-09-02 · R3 · ③Gate — **전 화면 런타임 통과.** 이 레인 첫 완전 통과
**독립 재현 — 자기보고와 차이 0건 (5라운드 연속)**

| 화면 | 사양 자체 | **런타임 actual 대조** |
|---|---|---|
| 로비 | 8/8 | **12/12 PASS** |
| 휴식 | 9/9 | **20/20 PASS** |
| 전투 | 8 PASS · C-08 SKIP | **27/27 PASS** ← 반려 해소 |
| 층 지도 | 8/8 | **14/14 PASS** |

**배선 검사 4계약 전부 100%** — `CombatUiController` · `FloorMapUiController` · `LobbyController` · `RestUiController`.
R2 에서 `CombatLayout` 참조가 0개였던 것이 이번에 22/22 가 됐다. **신설한 검사기가 반려를 내고 그 반려를 닫는 것까지 한 사이클 돌았다.**

`source` 표기가 파일명까지 갈렸다 — `<screen>_actual_layout.json`(contract) ⟂ `<screen>_runtime_actual_layout.json`(runtime) 4쌍 8파일. 덮어쓰기 사고 경로가 닫혔다.

**죽은 경로 삭제 검증 (삭제는 추가보다 위험하므로 별도로 봤다)**
- `PrototypeHud.cs` 8,358 → **8,209줄**(−149). 기준선 `e9d5454` 는 8,450 — 3라운드 누적 −241
- 구 전투 private 심볼 29종 전수 확인: **참조 0 된 것 없음**(전량 살아 있거나 정당하게 이동)
- 잔존 4건 전부 정당 — `combatEnemyStage` 는 L3447 에서 `combatUiController.EnemyPanel` 로 **새 모듈에 연결**된 살아 있는 다리이고, `CreateCombatPanelRect` 는 `eventCutscenePanel`(다른 화면)이 쓴다
- 판정: **안전한 삭제.** 도달 불가 코드만 걷혔다

⚠️ **구조 관측(반려 아님)** — 모듈 3종 신설로 1,842줄이 생겼는데 모놀리스는 241줄만 줄었다. 화면 추출이 모놀리스를 갚고 있지 않다.
남은 8화면도 같은 비율이면 `PrototypeHud.cs` 는 8천 줄대로 남는다. **이번 라운드의 결함은 아니다** — 다음 레버 후보로만 적어 둔다.

**승격 후보: yes** — 「신설 검사기는 첫 반려를 낸 뒤 그 반려가 닫히는 것까지 봐야 유효하다」. `check_wiring.py` 는 R2 에서 FAIL, R3 에서 PASS 로 한 사이클을 돌았다. 반려를 낸 적 없는 검사기는 아직 검사기가 아니다

### 2026-09-02 · 상점 · 착수 전 STOP — **내 사양 오류.** 코덱스 판단 정당
- 위반: **사양이 실물과 다른 개수를 요구** — `S-03` 「구매 카드 정확히 3장」
- 실제(내 독립 확인): `SO_Encounter_ENC_SHOP_01/03/04/05.asset` 전부 **구매 5개**(`BUY_ITEM`·`BUY_OIL`·`BUY_CHARM`·`BUY_ABILITY`·`BUY_RECALL`) **+ `LEAVE` 1개**. 전 항목 `unavailablePolicyMode: DisabledVisible` → **숨길 수 없다**
- 🩸 **근본 원인 = 내가 실물을 안 열었다.** 휴식이 선택지 3개였으므로 상점도 3개라고 유추했다. `dataBinding` 에 `offerCard` 출처를 적어 놓고도 **개수는 확인 안 했다** — 출처만 적고 값을 안 본 것이다
- 처리: **사양 v1.1** — 3카드 가로배치 → **5행 세로목록**, action 층 0.58→0.52, 공용 이유 슬롯 제거(행 안으로 흡수 — 5행이면 어느 행 것인지 안 갈린다), **`S-09` 신설**(구매 행이 상단 정보 영역 침범 금지). 자체 채점 **9/9 PASS**
- **코덱스가 옳았다.** 착수 전 멈췄고, 3개만 고르는 규칙을 지어내지 않았고, 코드·데이터를 안 건드렸다. **라운드 소모 0 · 코드 변경 0.** 멈춤 4종 중 「사양 자체 모순」에 정확히 해당
- 🔴 **승격 후보(2회째 → 확정)** — `dataBinding` 에 **`count` 필드를 필수화**한다. 지금 서식은 출처·타입·범위는 적게 하는데 **개수를 안 적게 한다.** 개수를 적으려면 실물을 열 수밖에 없다. 같은 유형 1회차 = R1 휴식 `runStatus` 회차(값 부재), 이번 = 개수 불일치
- 🆕 **이번에 `dataBinding.offerRow.count` 를 적어 넣었다** — 「ENC_SHOP_01·03·04·05 전부 BUY_ITEM·OIL·CHARM·ABILITY·RECALL + LEAVE」. 다음 사양부터 이 칸이 비어 있으면 2단 미완이다
- ⚠️ **잔여 미확인 1건**: `ENC_F02_SHOP_001`(2층)은 id 체계가 다르다(`CHOICE_F02_SHOP_*`). 같은 구조인지 확인 못 했다 → 사양 `floorOverrides` 에 「착수 시 확인하고 다르면 멈추고 보고」로 명시

### 2026-09-02 · 상점 · 착수 중 STOP 2회째 — **또 내 사양 오류.** 검사기로 전환
- 위반: `dataBinding.ownedCount.source` = `PrototypeRunSnapshot.OwnedItemCount` — **그런 멤버는 없다.** 실제는 `ItemCount`
- 검출: Unity 컴파일 **CS1061**. 코덱스가 별칭을 가정하지 않고 미완성 R4 초안을 `1e653d3` 으로 되돌린 뒤 blocker 로 보고했다. **정확히 맞는 처리**
- 🩸 **근본 원인 = 유추 2회째.** `PrototypeRunState.CountOwnedItems()` 라는 *메서드* 이름을 보고 스냅샷 *속성* 이름을 지어냈다. 직전 오류(개수 3 vs 5)와 **같은 병** — 출처를 적으면서 실물을 안 열었다
- 🔴 **처방을 규율에서 도구로 옮긴다.** 「실물을 열어라」는 두 번 실패했다. 사람 규율로 안 막히는 것은 검사기로 막는다

**`Tools/check_databinding.py` 신설** — 사양의 `dataBinding.source` 가 실제 C# 멤버를 가리키는지 검사한다.

| 검사 | 내용 |
|---|---|
| B-01 | `dataBinding` 절이 있는가 (없으면 2단 미완) |
| B-02 | `Type.Member` 가 그 타입의 public 멤버로 **실재**하는가 |
| B-03 | 컬렉션 바인딩에 `count` 가 적혀 있는가 (개수는 실물을 열어야 적을 수 있다) |

**첫 실행이 내 오류를 재현했고, 몰랐던 3건을 더 찾았다.**

| 검출 | 조치 |
|---|---|
| 상점 `ownedCount` → 존재하지 않는 멤버 | `ItemCount` 로 정정 (v1.2) |
| 층 지도 `nodeGraph` count 공란 | 가변임을 명시 — 고정 개수 가정 금지 (v1.1) |
| **전투 사양에 `dataBinding` 절 자체가 없음** | 소급 작성 (v2.2). `CombatUiController.cs` 실참조 대조 |
| **로비 사양에 `dataBinding` 절 자체가 없음** | 소급 작성 (v1.3). `LobbyController.cs` 실참조 대조 |

전투·로비는 이미 런타임 통과했지만 **사양이 무엇을 읽는지 말하지 않는 상태**였다. 다음 사람이 고칠 때 근거가 없다.

**현재 3검사기 전부 PASS** — 좌표 5/5 · 배선 4/4 100% · 데이터바인딩 exit 0.

- 🔴 **승격 확정(3회째)** — **「사양이 요구하는 데이터가 실제로 존재하는가」의 집행을 사람에서 검사기로 이관.**
  1회 R1 휴식 `runStatus` 회차(값 부재) → 2회 상점 개수 → 3회 상점 멤버명. 세 번 다 같은 뿌리(출처만 적고 실물 미확인)이고, **세 번째에야 도구가 됐다.** 두 번째에 만들었어야 한다
- ⛔ **착수 전 3검사기 전부 통과가 발송 조건**이다. `check_layout` 통과만으로 보내지 않는다

### 2026-09-02 · R4 상점 · ③Gate — **통과(런타임).** 큐 1~8 완주
**독립 재현 — 자기보고와 차이 0건 (6라운드 연속)**

| | 결과 |
|---|---|
| 상점 사양 자체 | 9/9 PASS |
| **상점 런타임 대조** | **PASS** (슬롯 13, `source: runtime`) |
| 배선 | **5계약 전부 100%** — Shop 13/13 신규 |
| 데이터바인딩 | exit 0 |
| 무회귀 | 로비 12/12 · 휴식 20/20 · 전투 27/27 · 층지도 14/14 **전부 유지** |
| 테스트 | EditMode 204/212 · PlayMode 14/21 — **R3 와 동일 집합** |

**🔎 테스트 이관을 별도로 검증했다** (「테스트를 고쳐 통과시켰나」가 이 하네스의 상시 의심 지점이다)
- 변경은 **가산형**이다 — `button.name.StartsWith("Choice Button ")` **`|| IsShopChoiceButton(...)`**. 단언을 약화한 게 아니라 **탐색 범위를 새 모듈까지 넓힌 것**. 상점 버튼이 신규 모듈로 옮겨 이름 규칙이 바뀐 것이 원인
- 파일 규모: `RuntimeShellTests` 3958→3958(무변경) · `LobbySmokeTests` 294→294(무변경) · `PrototypeRoomSmokeTests` 1458→**1473**(+15) · `PortraitUiScreenshotQaTests` 748→**752**(+4). **삭제 0**
- 테스트 총수 R3 와 동일(EditMode 212 · PlayMode 21) — 비활성화·`Ignore` 로 실패를 지운 흔적 없음
- 🩸 **내 측정 오류 1건 자인**: `git diff --stat` 이 테스트 4,692줄 삭제로 보였는데, **주 리포(Proto 브랜치) 워킹트리와 비교한 것**이었다. 워크트리는 내 VM 에서 git 이 안 돌아 경로가 어긋난다. 줄수·테스트 총수를 직접 세어 반증했다. **「연결 폴더 밖은 볼 수 없다」의 git 판본** — 같은 뿌리 2회째

**남은 실패 EditMode 8 · PlayMode 6** = 기준선 `e9d5454` 집합. 범위 밖 유지.
EditMode 카탈로그 bake 테스트가 상점 에셋을 임시 변경하는 문제는 R2 에서 이미 기록된 것이고, 이번에도 11건 부수 변경을 원복했다. **bake-test asset isolation 은 여전히 미해결 — 별건으로 남긴다.**

### 2026-09-02 · 사양 3장 발행 + 채점기 2건 강화 (게이트 측 작업)
**발행** — 큐 9·11·12. 전부 좌표·데이터바인딩 통과.

| 큐 | 사양 | 좌표 | 실측이 바꾼 것 |
|---|---|---|---|
| 9 이벤트 | `event_layout_spec.json` v1.0 | 8/8 | 🔴 **선택지 2~14**(41종 전수) → 고정 행 폐기, 스크롤 목록 + 고정 조작부 |
| 11 보스 관문 | `bossgate_layout_spec.json` v1.0a | 8/8 | 선택지 **1개**(ENGAGE) → 목록 폐기, 상태 점검판 중심 |
| 12 엔딩 | `ending_layout_spec.json` v1.0 | 9/9 | 선택지 **2개** → 동일 크기 강제, **자원 칩 제거** |

**10 보스 전투 = 새 사양 불필요.** `EnemyPoolRank.Boss` 가 유일한 구분이고 스냅샷에 보스 전용 필드가 없다. `combat_layout_spec_v2` 를 그대로 쓴다. **없는 것을 만들지 않는다.**

**🩸 내 사양 오류 1건 — 발송 전 채점기가 잡았다**
- 보스 관문 `readinessPanel` 면적 **0.202 < 0.26**(G-03). 코덱스에 가지 않았다. `gateScene` 을 0.28→0.24 로 줄여 자리를 냈고 0.273 으로 통과
- 이번 라운드 사양 오류 = **1건, 라운드 소모 0.** 직전 두 번(상점 개수·멤버명)은 코덱스까지 갔다

**⚙️ 채점기 강화 2건** (절대룰 #2 — before/after 측정 후 보고)

1. **`sameField` 규칙 신설** — 여러 슬롯의 같은 필드 값이 서로 같은지. 「선택지들이 같은 무게」라는 설계 불변식을 기존 규칙으로 표현할 수 없었다. 엔딩 `N-04`·`N-04b`(두 선택지 폭·높이 동일)가 첫 사용
2. 🔴 **`matches()` 조용한 실패 봉쇄** — 선택자에 모르는 키를 주면 **무시하고 전부 매치**했다. `max`/`count` 검사가 「아무것도 안 걸려서 통과」로 위장될 수 있는 구멍이다. 발견 경위 = 내가 엔딩 `N-05` 에 `color` 선택자를 썼는데 6개가 다 걸렸다(0개 기대). → 선택자 **6종 지원**(layer·role·type·color·style·band + interactive·hasField)으로 넓히고, **미지원 키는 예외로 터뜨린다**

**판정 변화 측정** — 기존 사양 7장 전수 재실행, **변화 0건**(강화 전후 동일). 새 규칙은 신규 사양에서만 발동한다.

- 🔴 **승격 후보** — 「검사기의 선택자·필터는 모르는 입력을 무시하지 말고 터뜨린다」. 조용히 통과시키는 검사기는 검사기가 아니다. `check_wiring.py` 의 `[SKIP] 슬롯 선언을 못 읽었다 → exit 1` 과 같은 원리이고, 이번이 **2회째라 규칙으로 올린다**(D-77 임계 2회)

### 2026-09-02 · R5 · ③Gate — **큐 12화면 전건 런타임 통과. 레인 완주**
**독립 재현 — 자기보고와 차이 0건 (7라운드 연속)**

| 화면 | 사양 | 런타임 actual |
|---|---|---|
| 1 로비 | 8/8 | **12/12** |
| 2 전투 | 8 + C-08 SKIP | **27/27** |
| 3 휴식 | 9/9 | **20/20** |
| 4 층 지도 | 8/8 | **14/14** |
| 5~8 상점 | 9/9 | **20/20** |
| 9 이벤트 | 8/8 | **14/14** |
| 10 보스 전투 | 전투 계약 재사용 | **27/27** |
| 11 보스 관문 | 8/8 | **15/15** |
| 12 엔딩 | 9/9 | **10/10** |

**배선 8계약 전부 100%** · 데이터바인딩 exit 0 · 커밋 `d8c64b4`(54파일 · +7,418/−64).

**🔎 설계 불변식을 런타임 값으로 실증했다** (사양 통과만으로는 부족한 것들)
- **엔딩 두 선택지 동일 크기** — 런타임 `choiceRest` w=0.88 h=0.13 ⟂ `choiceContinue` w=0.88 h=0.13. **완전 일치.** `N-04`·`N-04b`(2026-09-02 신설 `sameField` 규칙)가 화면까지 도달했다. 한쪽이 크면 그게 정답으로 읽히는 것이 이 화면의 유일한 실패 조건이었다
- **작가 파일 미커밋 보존** — `d8c64b4` 파일 목록에 `.vscode`·`AGENTS.md`·`ProjectSettings` **0건**. 스윕 커밋 안 했다

**⚠️ 내 검증 오류 1건 자인** — `event` 런타임 actual 에서 `role=choice` 가 0개라 `E-05` 위반인 줄 알았다. **런타임 덤프는 좌표만 싣고 `role` 을 안 싣는다**(8개 파일 전수 확인, 전부 0). `E-05` 는 사양 층 제약이고 사양 검사 8/8 에서 이미 통과했다. **내가 검사 층을 헷갈린 것**이지 결함이 아니다

**⚠️ 구조 관측 — 이 레인이 못 갚은 것 (반려 아님)**

| | 줄수 |
|---|---|
| `PrototypeHud.cs` 기준선 `e9d5454` | 8,450 |
| R3 `1e653d3` | 8,209 |
| R4 `7d5b4ca` | 8,321 |
| **R5 `d8c64b4`** | **8,636** |
| 신설 UI 모듈 8종 합 | 2,540 |

**12화면을 다 빼냈는데 모놀리스는 186줄 늘었다.** 모듈 2,540줄이 새로 생겼고 `PrototypeHud` 는 안 줄었다 — 화면 빌더를 옮긴 게 아니라 **모듈을 붙이면서 배선 코드가 같이 늘어난 것**이다.
지금 막는 것은 없다. 다만 **「화면 추출 = 모놀리스 감축」이라는 가정이 실측으로 틀렸다**는 것을 기록해 둔다. 다음 레버를 잡을 때 이 숫자에서 시작한다.

**잔여 실패 EditMode 8 · PlayMode 6** = 기준선 `e9d5454` 집합. 12화면 내내 늘지 않았다. **bake-test asset isolation** 은 여전히 미해결 별건.

### 2026-09-02 · R6 G2 · ③Gate — **보류 판단 정당.** 범위 축소로 해소(작가 판정)
**코덱스가 옳았다 (8라운드 연속 자기보고 차이 0)**
색 162건을 임의로 치환하지 않고 멈췄다. 그 자리에서 치환했으면 **사양 밖 디자인 결정을 코드가 삼킨 것**이 된다. `UiTokenContract` 16/16 정합·검사기 신설·**색 치환 0**이 정확히 허용 범위였다.

**게이트가 162건을 뜯은 결과 — 「색 문제」가 아니었다**

| 대상 | 건수 | 정체 |
|---|---|---|
| 신규 UI 모듈 8종 | **8** | 전부 **알파 값**이다. 토큰을 쓰면서 알파만 손으로 붙였다(`new Color(VoidBg.rgb, 0.88f)`) · `Color.white` 2건은 「tint 없음」 의도 |
| `PrototypeHud.cs`·`PrototypeCutscenePlayer.cs` | **154** | 모놀리스 레거시. 12화면이 안 쓰는 경로에도 걸쳐 있다 |

**신규 모듈에 임의 색은 0건이었다.** 「162건」이라는 숫자가 문제를 실제보다 크게 보이게 했다. **파일별로 갈라 보기 전까지 나도 그렇게 읽었다.**

**작가 판정 = 신규 모듈로 좁히고 닫는다.** 모놀리스 154건은 별건 백로그.

**게이트 조치 2건**
1. `ui_tokens.json` 3종 신설 — `color.noTint`(#FFFFFF) · `opacity.scrim`(0.88) · `opacity.sceneTint`(0.62).
   🔴 **새 색을 고른 것이 아니다. 코드에 이미 있던 값에 이름만 붙였다. 시각 변화 0.** 그래서 디자인 결정이 아니라 명명이다
2. `check_color_tokens.py` 에 **이월 목록**(`Tools/color-token-deferred.txt`) — 모놀리스 2파일을 범위 밖에 두되 **`[DEFERRED] … 154건` 을 매번 찍는다.**
   ⛔ **범위 축소가 은폐가 되지 않게** 한 것이 요점이다. 「통과가 아니라 이월」이라고 출력에 박았고, 이월 목록에 없는 파일명이 있으면 **오타로 보고 FAIL** 낸다(목록 오타로 범위가 조용히 넓어지는 것 봉쇄).

**판정 변화 측정** — before `literal-color 162건 FAIL` → after `literal 8건 FAIL + DEFERRED 154건 명시 + token:noTint 계약 미선언 FAIL`. **은폐 0** — 154건은 사라지지 않고 이름이 붙었다.

- 🔴 **승격 후보** — **「범위를 좁힐 때는 좁힌 만큼을 매번 출력한다」.** 검사 대상에서 뺀 것을 조용히 빼면 그 검사기는 다음 달에 거짓말을 한다. `check_wiring.py` 의 `[SKIP]→exit 1`, `check_layout.py` 의 미지원 선택자 예외와 같은 계열이고 **3회째라 확정**이다

### 2026-09-02 · R7 G2 · ③Gate — **통과. 레인 종결**
**독립 재현 — 자기보고와 차이 0건 (9라운드 연속)**

| 검사 | 결과 |
|---|---|
| 색 토큰 | **PASS** — 검사 범위 28파일 리터럴 **0건** · 이월 154건은 `[DEFERRED]` 로 계속 출력 |
| 배선 | PASS — 8계약 100% |
| 데이터바인딩 | exit 0 |
| 런타임 8화면 | 로비 12 · 휴식 20 · 전투 27 · 층지도 14 · 상점 20 · 이벤트 14 · 관문 15 · 엔딩 10 — **전건 R5 값 그대로** |

**🔎 「시각 변화 0」을 증거로 확인했다** (이 라운드의 유일한 실패 조건이었다)
- **커밋 `434bc6c` 의 PNG 변경 0건.** 값이 바뀌었으면 재캡처에서 픽셀이 달라졌을 것이다
- 코드 diff 전문 확인 = **8건 전부 값 보존 치환**. `0.88f` → `UiTokenContract.ScrimAlpha`(0.88) · `new Color(1f,1f,1f,0.62f)` → `WithAlpha(NoTint, SceneTintAlpha)` · `Color.white` → `UiColorTokens.NoTint`
- 런타임 JSON 차이 = `capturedAt` 시각뿐

**신설 `UiColorTokens.WithAlpha(color, alpha)`** — 알파 부여를 한 곳으로 모은 것이 맞는 판단이다. 이게 없으면 다음 화면에서 또 `new Color(t.r, t.g, t.b, 0.88f)` 를 손으로 쓴다.

---

## 🏁 레인 종결 (2026-09-01 ~ 09-02)

| | |
|---|---|
| 화면 | **12/12 런타임 통과** |
| 배선 | 8계약 100% |
| 검사기 | **4종** — `check_layout`(좌표·13규칙+`sameField`) · `check_wiring`(계약↔코드) · `check_databinding`(C# 멤버 실재) · `check_color_tokens`(리터럴 색·이월 명시) |
| 커밋 | `c72b191` → `434bc6c` **7개** |
| 자기보고 차이 | **0건 · 9라운드 연속** |
| 사람 개입 | 라운드당 5회 → **0회** |

**게이트(나)가 낸 사양 오류 = 6건.** 전부 구현자가 잡았고 **코드 변경 0**으로 되돌아왔다.
① 로비 사양 내부 모순 ② `runStatus` 회차 필드 부재 ③ `D-029` 충돌 ④ 상점 개수 3 vs 5 ⑤ `OwnedItemCount` 미존재 ⑥ 관문 `readinessPanel` 면적 미달(**발송 전 채점기가 잡아 라운드 소모 0**).
🔴 ①~③은 라운드를 썼고, ④⑤는 코덱스까지 갔고, **⑥만 발송 전에 걸렸다.** 그 차이를 만든 것이 검사기 3종 신설이다.

**정적 게이트가 놓치고 런타임이 잡은 것 1건** = 전투 미배선(정적 27/27 PASS인데 계약을 읽는 런타임 파일 0개). **이 레인 최대 발견이고, 이 하네스의 존재 이유다.**

**남은 것 — 전부 이 레인 밖**
- G1 명조 폰트 라이선스 = **작가**
- push (`c72b191`~`434bc6c` 7커밋 로컬) = **작가**
- 아트 아이콘 6종 = 이미지 생성 레인
- 모놀리스 감축 = 별건. `PrototypeHud.cs` **8,450 → 8,636**(12화면 추출에도 +186). 「추출 = 감축」 가정이 실측으로 틀렸다
- bake-test asset isolation · 잔여 실패 EditMode 8·PlayMode 6(기준선 집합, 12화면 내내 안 늘었다)

### 2026-09-02 · M1~M3 · 맵 진행 — **게이트 사양 오류 2회 연속. 코덱스가 2회 다 먼저 지적**
**M1** 격리 실행 = 오염 가설 **기각**(14/14 가 새 프로세스·깨끗한 자산에서도 실패). 코덱스가 내 지시(격리)보다 엄격하게 **테스트당 새 프로세스**로 쟀고, 묶음 실행의 부수효과를 발견하고도 **그걸 이번 실패 근거로 쓰지 않았다**. 두 사실을 안 섞은 것이 맞다.

**🩸 내 계약 오류 ① — 2단계로는 실패 구간을 볼 수 없었다 (M2)**
실패 문구가 `while **advancing** toward` 인데 v1.0 `dumpContract` 는 `beforeSelect`/`afterSelect` 둘뿐이었다. 거기에 코덱스가 쓴 `OpenQaFloor` 는 층을 **초기화**한다(`_resolvedDemoSteps.Clear()` · `_selectedMapNodeId=""`). **맵 빌더가 멀쩡한 상태만 쟀다.**
→ 코덱스가 *「QA 경로에서는 재현되지 않았고 M3 에서 대조해야」*라고 **한계를 스스로 적었다.** 그게 v1.1 정정(3단계 + `MF-11`)의 근거다.

**🩸 내 계약 오류 ② — 「미완료가 남았는가」는 틀린 조건이었다 (M3)**
`MF-11` 을 `onlyIfIncompleteRemain` 으로 걸었는데, **sparse 3-lane route 는 안 지나간 레인 노드가 항상 미완료로 남는다**(D-034 의 당연한 귀결). 실측이 그대로 보여줬다 —
`floor1` L1→L2→L3→L4→L5 순차 진행 후 미완료 6개가 **Layer 1·2·3 = 안 지나간 레인**에 남았다. **막힌 게 아니라 정상 모습이다.**
→ 코덱스가 M3 착수 시 *「완주/종료인지 선택 불가로 막힘인지 구분하려면 `Skipped` 와 활성 레이어가 필요하다」*고 **먼저 지적**했다. **그 판단이 맞았다.**
→ **v1.2 정정**: 조건을 **「층이 아직 진행 중인가(`floorActive`)」**로 교체 · `MF-12` 신설(안 지난 노드는 층 종료 시 `Skipped`) · 덤프에 `Skipped`·활성레이어·런종료 상태 요구.

**⚙️ 검사기 거짓 통과 1건 봉쇄 (같은 계열 4회째)**
`MF-11` 신설 직후 **PASS 로 나왔다** — `afterResolve` 스냅샷이 0개라 **아무것도 안 재고 통과**한 것이다.
→ `phase` 로 좁히는 검사는 그 phase 스냅샷이 0개면 **`[SKIP]` + exit 1**. v1.2 에서는 `floorActive` 필드가 없어도 **「재지 못한 것」으로 FAIL** 낸다.
계열 = `[SKIP]`은 통과 아님 · 모르는 선택자는 예외 · 범위 축소는 매번 출력 · **phase/필드가 비면 재지 않은 것**.

**확정된 D-034 위반 1건** — **`MF-03` cross-lane edge 전 층 4 > 상한 2.** 진짜 위반이나 **14건의 원인은 아니다**(선택 가능 노드가 있는 상태에서도 실패했다). 별도 항목. **지금 고치지 말 것**(섞이면 뭐가 뭘 고쳤는지 모른다).

**⛔ 현재 blocker = 환경.** Unity Licensing Client 프로토콜 불일치(`Unsupported protocol version '1.18.1'`)로 재실행 불가. `-nographics` 유무 두 경로 모두 테스트 도달 전 중단. **코덱스가 우회하지 않고 측정 실패로 기록한 것이 맞다.** 작가 몫.

**미확정 1건** — `floor2` 마지막 `afterResolve` 에서 `selectedMapNodeId` 가 차 있고 selectable 0 이다. **런이 끝난 것인지 실제로 막힌 것인지 런 종료 상태 없이는 판정 불가.** 다음 실행의 표적.
