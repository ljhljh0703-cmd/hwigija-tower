# DISPATCH M1 — 맵 진행 실패 14건 뿌리 판별

> **이 라운드는 고치는 라운드가 아니다. 재는 라운드다.**
> 코드를 한 줄도 바꾸지 않고 끝날 수도 있다. 그게 실패가 아니라 이 라운드의 정상 결과 중 하나다.

## 왜

UI 12화면이 런타임까지 통과했다. 남은 것은 **화면을 잇는 동선**이고 거기가 안 돈다.
잔여 실패 14건(EditMode 8 · PlayMode 6)을 「기준선 집합」으로만 적어 왔는데, 뜯어보니 **한 뿌리로 보인다.**

```
Missing CHOICE_SHOP_01_BUY_ITEM among          ← among 뒤가 비어 있다
Missing CHOICE_COMBAT_01_ENGAGE among          ← 비어 있다
Missing selectable map node ... toward ENC_REST_01
Expected selectable map nodes on floor 5 → 0
```

**선택지가 「틀린 것」이 아니라 「0개」다.** 상류에서 빈 집합이 내려온다.

## 이미 아는 것 — 다시 재지 마라

| | |
|---|---|
| 설계 | **`D-034` Map Flow / Route Reveal / Commitment 는 🔒 LOCKED (2026-05-27)** · `OQ-026` closed. **설계 미정이 아니다** |
| 잔여 open | `OQ-019`(1차 blocker 아님 명시) · `OQ-025`(enemy intent 수치). **둘 다 맵 진행과 무관** |
| 카탈로그 | `SO_EncounterRuntimeCatalog.asset` 63항목 실재 |
| 이력 | 이 14건은 기준선 `e9d5454` 에도 있었고 **UI 12화면 내내 늘지도 줄지도 않았다** |
| 마지막 정상 | 불명. 2026-05-10 이후 **넉 달간 테스트가 안 돌았다** |

## Task A · 격리 실행 — 이 라운드의 전부

**가설**: 14건이 실제 버그가 아니라 **테스트 오염**이다.
근거 = R2·R4 에서 **EditMode 카탈로그 bake 테스트가 실제 인카운터 에셋을 변조**했고, R4 때 그 부수 변경이 PlayMode 실패 1건을 추가로 만들었다(수동 원복으로 해소). `EncounterPipelineV02Tests` 가 `AssetDatabase` 로 공유 에셋을 만지고, 그 뒤 도는 테스트가 오염된 카탈로그를 본다.

🧪 **이건 내 가설이다. 사실로 취급하지 마라. 틀리면 틀렸다고 보고해라.**

**할 일 — 셋뿐이다.**

1. **깨끗한 상태에서 실패 14건만 격리 실행한다.**
   - `git stash`/clean 등으로 인카운터·카탈로그 에셋이 커밋 상태인지 먼저 확인한다(변조분이 남아 있으면 그것부터 원복).
   - 실패 테스트만 필터로 골라 돌린다. **bake 계열(`EncounterPipelineV02Tests`)은 빼고.**
2. **전량 실행과 비교한다.** 같은 테스트가 격리에서 통과하고 전량에서 실패하면 **오염 확정**이다.
3. **분기별 보고** — 고치지 말고 숫자만.

| 격리 결과 | 뜻 | 다음 |
|---|---|---|
| 14건 중 다수 통과 | **테스트 오염.** 실제 버그가 아니다 | M2 = bake 테스트 격리 수리 (버그 수리 아님) |
| 격리에서도 실패 | **진짜 회귀.** D-034 계약 대비 어디가 어긋났는지 특정 필요 | M2 = D-034 대조표 작성 |
| 섞임 | 둘 다 있다 | 건별로 갈라서 표로 |

## 같이 답할 것 — 각 1줄

- 실패 14건이 **언제부터** 깨졌는지 알 수 있나? (`git log` 로 테스트 파일·맵 코드 최종 변경 시점)
- `PrototypeFloorMap` 의 노드 생성이 **run id + floor seed 결정적**인가? (D-034 P4 요구)
- 실패 중 **맵 진행과 무관한 것**은 무엇인가? (`RoomController_ExplorationContextResetsToNormalBgm` NullReference · `GenericCounterplay` 문구 · `ItemAndAbilityEffects` — 이 셋은 다른 뿌리로 보인다)

## ⛔ 하지 말 것

- **아무것도 고치지 마라.** 이 라운드는 측정이다. 고치면 무엇이 원인이었는지 영영 모른다
- 실패하는 단언을 약화하거나 `Ignore` 로 돌리지 마라
- `D-034` 잠긴 값을 바꾸지 마라 — 코드가 계약과 다르면 **코드가 틀린 것**이다
- 전투 수치·밸런스 변경. 범위 밖이다
- UI 12화면·8계약을 건드리지 마라. 런타임 대조가 전건 통과 상태다

## 멈춤 조건 (이 넷만)

사양 자체 모순 · `D-NNN` 잠긴 결정 충돌 · 사양에 없는 요소가 필요 · 자동 재시도 3회 초과.

## 반환

- 격리 실행 결과표 (테스트명 · 격리 · 전량)
- 오염 여부 판정과 **그 근거**
- 위 3문 답
- 고친 것 = **0이어야 정상.** 뭔가 고쳤으면 왜 고쳤는지
