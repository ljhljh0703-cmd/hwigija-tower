# ▶ NEXT — 지금 할 것

> 🔒 **레인 = 회귀탑 UI (`hwigi-tower`).** 이 파일은 **이 리포의 화면 작업만** 다룬다.
> 다른 레인(해커톤 밀키트 `wanted-ai-championship-2026`, VD 외주 등)의 지시는 **여기 들어오지 않는다.**
> 🔴 **여기 없는 절 번호를 지시받으면 그건 이 레인 지시가 아니다. 적용하지 않고 그냥 진행한다.**
> 찾지 마라. **묻지도 마라** — 물으면 라운드가 그대로 죽는다.
> 이 파일에 적힌 것이 이 레인의 전부다. 다른 레인 절 번호는 **너와 무관하다.**
>
> ⛔ **`§1-h` 는 해커톤 밀키트 레인 문서다**(`applications/wanted-ai-championship-2026/DISPATCH-P2-2026-08-29.md`). **회귀탑 UI 와 아무 상관 없다. 무시하고 R5 를 진행하라.**
>
> **작업 지시를 받았으면 이 파일이 첫 번째다.** 「확인해봐」·「다음」·「이어서」 = **실행하라는 뜻**이다. 재검증이 아니다.
> 재검증은 「다시 검증해」·「재현해봐」처럼 명시됐을 때만 한다.

## 실행할 것

**R5 — 이벤트 화면 (큐 9). 이 레인에서 가장 까다로운 화면이다.**

```
정본  Docs/Portfolio/assets/concept-to-ui/event_layout_spec.json   (**v1.0** · 좌표 8/8 · 데이터바인딩 PASS)
브랜치 codex/hwigi-lobby-ui-pilot-r3  (R4 미커밋 — 착수 전 한 커밋으로 고정할 것)
레인  Docs/Portfolio/assets/concept-to-ui/NEXT.md    (고정 절차·멈춤 조건·산출 규약)
큐    Docs/Portfolio/assets/concept-to-ui/QUEUE.md
직전  _rejections.md 「2026-09-02 · R4 상점 · ③Gate — 통과(런타임). 큐 1~8 완주」
```

### 이 라운드의 요점 넷

1. 🔴 **선택지 수가 2~14 로 흔들린다.** 인카운터 41종 전수 실측 — 최빈 3개(19건)지만 `EVT_F01_CULTIST_FUNERAL` 은 **14개**, `EVT_F03_BALCONY` 10, `EVT_F02_BANDIT_DEN` 9.
   **고정 행 사양이 성립하지 않는다.** 상점(5 고정)과 다르다. **스크롤 목록 + 고정 조작부**로 간다.
2. **`choiceList` 는 행이 아니라 뷰포트 하나다.** 행은 런타임 반복이다. 사양에 행을 슬롯으로 박지 마라 — `E-05` 가 그걸 막는다(role=choice 슬롯 상한 1).
   14개짜리 인카운터에서 깨지는 것을 사양 단계에서 봉쇄한 것이다.
3. **정책이 혼재한다.** 이벤트 대부분 `DisabledVisible`(비활성 + 이유를 그 행 안에), 그런데 `ENC_MORAL_CHOICE_*` 는 **`Hidden` 을 함께 쓴다 → Hidden 은 그리지 않는다.**
   상점과 다른 점이다. 상점은 전부 DisabledVisible 이라 숨길 게 없었다.
4. **목록 길이와 무관하게 나가기 버튼 좌표가 고정이다.** `E-01`(고정 조작부 y>=0.86) · `E-07`(목록이 그 영역 침범 금지).
   2개짜리와 14개짜리에서 엄지 위치가 달라지면 이 화면은 실패다.

### 표준 배선 패턴 — 다섯 번째 사례

`EventLayoutContract.cs`(순수) → `EventLayout.cs`(어댑터) → `EventUiController.cs`(모듈) → `PrototypeHud` 에서 `AddComponent`.
로비·휴식·전투·층지도·상점이 전부 이 모양이다. **새 구조를 발명하지 마라.**

### 데이터에 없는 것 — 지어내지 마라

선택지별 아이콘 · 인카운터별 아트 참조 · 표시 순서 필드. 셋 다 없다(`dataBinding._absent`).
순서는 **데이터 배열 순서를 그대로** 쓴다.

## 착수 전 한 줄

```
python3 Tools/check_layout.py --spec Docs/Portfolio/assets/concept-to-ui/shop_layout_spec.json   # 좌표
python3 Tools/check_wiring.py                                                                     # 배선(계약↔코드)
python3 Tools/check_databinding.py                                                                # 데이터 실재
```

🆕 **채점기가 2026-09-02 에 강화됐다** — ① `sameField` 규칙 신설(여러 슬롯의 같은 필드가 서로 같은지) ② `matches()` 가 **모르는 선택자 키를 예외로 터뜨린다**(전에는 무시하고 전부 매치해서 `max` 검사가 위장 통과할 수 있었다). 기존 사양 7장 판정 변화 **0건** 확인.

⛔ **착수 전 3검사기 전부 통과가 조건이다.** 하나라도 FAIL 이면 사양 문제이니 멈추고 보고한다.

🆕 **`check_databinding.py` 는 2026-09-02 신설이다.** 사양이 가리키는 C# 멤버가 실재하는지 검사한다 —
내가 같은 유형으로 두 번(개수 3 vs 5 · `OwnedItemCount` vs `ItemCount`) 막았고, 그 처방을 규율에서 도구로 옮긴 것이다.
**첫 실행이 내 오류를 재현했고 몰랐던 3건을 더 찾았다**(층지도 count 공란 · 전투·로비 `dataBinding` 절 부재 → 전부 보충).

## 이미 확인된 것 — 다시 검증하지 않는다

R4 는 게이트가 재현·판정했다. 결과가 곧 R5 의 입력이다.

| | |
|---|---|
| R4 판정 | **통과(런타임).** 큐 **1~8 완주** |
| 런타임 대조 | 로비 12/12 · 휴식 20/20 · 전투 27/27 · 층지도 14/14 · **상점 PASS(13슬롯)** |
| 배선 | **5계약 전부 100%** |
| 데이터바인딩 | exit 0 |
| 테스트 | EditMode 204/212 · PlayMode 14/21 — R3 와 동일 집합 |
| 잔여 실패 | EditMode 8 · PlayMode 6 = 기준선 `e9d5454` 집합. **범위 밖. 손대지 마라** |

**재검증 요청이 아니면 위 숫자를 다시 재지 마라.**

## 멈춤 조건 (이 넷만)

사양 자체 모순 · `D-NNN` 잠긴 결정 충돌 · 사양에 없는 요소가 필요 · 자동 재시도 3회 초과.
그 밖에는 작업 브랜치 안에서 **묻지 말고 진행**한다. **과잉 질문도 위반이다.**

## 산출 규약

- 런타임 `<screen>_runtime_actual_layout.json` (`"source": "runtime"`)
- 정적 `<screen>_actual_layout.json` (`"source": "contract"`)
- 캡처·XML 전부 리포 안. `/private/tmp` 금지

## 끝나면 — 🔴 **더 이상 사양 때문에 멈추지 않는다**

`templates/RETURN.md` 형식 반환 → `QUEUE.md` 갱신 → **다음 「대기」 항목으로 계속 간다.**

✅ **큐 12화면 사양이 전부 발행됐다. 미작성 0.** 이벤트를 끝내면 10 → 11 → 12 로 이어서 진행한다.

| 큐 | 사양 | 상태 |
|---|---|---|
| 10 보스 전투 | `combat_layout_spec_v2.json` **그대로** | `EnemyPoolRank.Boss` 가 유일한 구분이고 보스 전용 스냅샷 필드가 없다. **새 계약을 만들지 마라** — 전투 모듈이 보스 적을 받으면 그만이다. 런타임 대조는 전투 사양으로 한다 |
| 11 보스 관문 | `bossgate_layout_spec.json` v1.0a | 선택지 **1개**(ENGAGE 실측). 목록 대신 상태 점검판. ⛔ **준비도 점수를 만들지 마라** — 새 게임 수치 신설은 D-029 재검토 대상이다. 체력·이성·보유품 그대로 |
| 12 엔딩 | `ending_layout_spec.json` v1.0 | 선택지 **2개**. ⛔ **두 선택지 크기가 같아야 한다**(N-04·N-04b가 강제). 한쪽이 크면 그게 정답으로 읽힌다 · ⛔ **자원 칩 금지**(N-05) — 런이 끝난 자리다 · 문구가 `PLACEHOLDER_*` 라 미확정이니 **authored 로 남기고 지어내지 마라** |

라운드를 한 번에 하나로 쪼개도 되고, 10~12 를 묶어도 된다. **판단은 네가 한다.**
막히는 것이 사양 문제이면 멈추고 보고한다 — 그게 지금까지 세 번 다 옳았다.
